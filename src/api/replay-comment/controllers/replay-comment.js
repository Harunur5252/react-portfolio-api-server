'use strict';

/**
 * replay-comment controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::replay-comment.replay-comment');
